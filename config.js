// Edit this URL when your API changes. No build step — this is plain HTML/JS.
// On Vercel, `/api/*` is rewritten to the backend (see `vercel.json`), which avoids CORS/preflight issues.
// For local dev without Vercel rewrites, set this to your backend URL (e.g. "http://127.0.0.1:8000").
window.API_BASE_URL = "";
